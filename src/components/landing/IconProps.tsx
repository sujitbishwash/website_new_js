export type IconProps = {
  path: string;            // SVG path data is a string
  className?: string;      // optional, defaults to "w-6 h-6"
  strokeWidth?: number;    // optional, defaults to 1.5
  useFill?: boolean;       // optional, defaults to false
};
